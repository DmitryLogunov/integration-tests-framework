<%- include('partials/header') %>

<div class="container-fluid">

    <!-- Main area -->
    <div class="card mb-3">
        <div class="card-header">
            <i class="fas fa-chart-area"></i>
            <%= title %></div>
        <div class="card-body">
            <% if (typeof reportID !== 'undefined' ) { %>
            <iframe src="/show-report?report=<%= reportID %>" width="100%" height="700" style="border: none;"></iframe>
            <% } %>
        </div>
    </div>
</div>
<!-- /.container-fluid -->

<%- include('partials/footer') %>
